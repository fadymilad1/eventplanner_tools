export const environment = {
  production: true,
  // Backend URL - accessible from browser
  // In Docker, backend is accessible via the host machine's localhost
  apiUrl: 'http://localhost:5000/api'
};

